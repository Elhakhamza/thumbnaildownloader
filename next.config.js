/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['img.youtube.com'], // Allow YouTube image domain
    },
  };
  
  module.exports = nextConfig;
  